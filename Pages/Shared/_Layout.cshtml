@{
    var _path = Context?.Request?.Path.Value ?? string.Empty;
    var _isUserArea = _path.StartsWith("/User", System.StringComparison.OrdinalIgnoreCase);
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Barangay Health Care </title>
    
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" asp-append-version="true" />
    <!-- Custom CSS -->
    <link rel="stylesheet" href="~/css/custom-site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/custom.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/doctor-layout.css" asp-append-version="true" />
    <!-- Theme: Orange + White (not used - keeping for reference) -->
    <link id="original-theme" rel="stylesheet" href="~/css/theme-orange.css" asp-append-version="true" disabled />
    <!-- Enhanced Theme (applied directly) -->
    <link id="enhanced-theme" rel="stylesheet" href="~/css/enhanced-theme.css" asp-append-version="true" />
    @if (_isUserArea)
    {
        <link rel="stylesheet" href="~/css/user-theme.css" asp-append-version="true" />
    }
    
    @await RenderSectionAsync("Styles", required: false)
</head>
<body class="@(_isUserArea ? "user-theme" : null) enhanced-theme d-flex flex-column min-vh-100">
    <header>
        <nav class="navbar navbar-expand-md navbar-dark bg-primary">
            <div class="container-fluid">
                <a class="navbar-brand d-flex align-items-center" href="/">
                    <i class="fas fa-heartbeat me-2"></i>
                    <span>BHCARE System</span>
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarCollapse">
                    <ul class="navbar-nav ms-auto mb-2 mb-md-0">
                        @if (Context.Request.Path.Value?.Contains("/Account/ForgotPassword") == true || 
                             Context.Request.Path.Value?.Contains("/Account/VerifyOTP") == true || 
                             Context.Request.Path.Value?.Contains("/Account/OTPVerification") == true ||
                             Context.Request.Path.Value?.Contains("/Account/ResetPassword") == true ||
                             Context.Request.Path.Value?.Contains("/Account/Login") == true ||
                             Context.Request.Path.Value?.Contains("/Account/SignUp") == true ||
                             Context.Request.Path.Value?.Contains("/Account/VerifyEmail") == true ||
                             Context.Request.Path.Value?.Contains("/Account/PendingApproval") == true ||
                             Context.Request.Path.Value?.Contains("/Account/WaitingForApproval") == true)
                        {
                            <li class="nav-item">
                                <a class="nav-link text-white" href="/">
                                    <i class="fas fa-arrow-left me-1"></i> Back to Home
                                </a>
                            </li>
                        }
                        else if (Context.Request.Path.Value?.Contains("/Account/AdminLogin") == true)
                        {
                            <!-- No navigation items for admin login page -->
                        }
                        else
                        {
                            <li class="nav-item">
                                <form class="form-inline" asp-page="/Account/Logout" method="post">
                                    <button type="submit" class="nav-link btn btn-link text-white">Logout</button>
                                </form>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <main role="main" class="pb-3 flex-grow-1">
        @RenderBody()
    </main>

    <footer class="footer border-top text-muted">
        <div class="container">
            &copy; 2024 - BHCARE System - <a asp-page="/Privacy">Privacy</a> - <a asp-page="/Terms">Terms</a> - <a asp-page="/DataPrivacy">Data Privacy</a>
        </div>
    </footer>

    <!-- Bootstrap Bundle with Popper -->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Custom JavaScript -->
    <script src="~/js/site.js" asp-append-version="true"></script>
    <!-- Theme Switcher -->
    <script src="~/js/theme-switcher.js" asp-append-version="true"></script>
    
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
